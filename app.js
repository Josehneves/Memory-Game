//level 1

let score = 0;
let firstSelection = "";
let secondSelection = "";

const grid1 = document.querySelectorAll(".grid1 .cards");
grid1.forEach((cards) => {
    cards.addEventListener("click", () => {
       console.log(true);
    });
});